[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "quilt-fleet-publish"
version = "0.1.0"
description = "Publish the fleet"
readme = "README.md"
requires-python = ">=3.8"

[tool.setuptools]
packages = ["quilt_fleet_publish"]
